# System Gateway Dependencies
# Minimal footprint for sandboxed execution environment

# Web framework
fastapi>=0.109.0,<1.0.0
uvicorn[standard]>=0.27.0,<1.0.0

# Filesystem monitoring
watchdog>=4.0.0,<5.0.0

# Validation
pydantic>=2.5.0,<3.0.0
pydantic-settings>=2.1.0,<3.0.0

# HTTP client for health checks (already in base image usually)
httpx>=0.26.0,<1.0.0

